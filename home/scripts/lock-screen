#!/usr/bin/env bash
# 用 Noctalia 锁屏（依赖 Noctalia 正在运行）。

set -euo pipefail

if ! command -v noctalia-shell >/dev/null 2>&1; then
  echo "lock-screen: noctalia-shell not found in PATH" >&2
  exit 1
fi

exec noctalia-shell ipc call lockScreen lock
